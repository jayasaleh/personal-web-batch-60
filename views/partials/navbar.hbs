<nav class="navbar navbar-expand-lg bg-body-tertiary" style="background-color: #e2e2e2 !important;">
  <div class="container">
    <a class="navbar-brand" href="/"><img
        src="/assets/img/icon.png"
        alt="logo"
        style="width:45px"
      /></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link
          {{#if (equal currentPage 'Home')}}
          actived
          {{/if}}
          " aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link
          {{#if (equal currentPage 'Project List')}}
          actived
          {{/if}}" href="/my-project">My Project</a>
        </li>
        <li class="nav-item">
          <a class="nav-link 
          {{#if (equal currentPage 'Testimonials')}}
          actived
          {{/if}}" href="/testimonials">Testimonials</a>
        </li>
      </ul>
      <div class="d-flex align-items-center gap-3" id="right-nav">
        {{#if user}}
        <span class="me-3 text-center">Selamat datang {{user.name}}</span>
        <a href="/logout" class="btn btn-danger" >Log Out</a>
        {{else }}
        <a href="/login" class="btn " style="background-color: orangered !important; color:white;">Log In</a>
        {{/if}}
        <a class="btn" id="btn-black" style="background-color:
          {{#if (equal currentPage 'Contact Us')}}
           orangered
          {{/if}};" href="contact-me">Contact Us</a>
      </div>
    </div>
  </div>
</nav>